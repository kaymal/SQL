---
title: "SQL Queries with R Notebook"
author: "Kaymal"
output: github_document
---
# SQL Queries with R Notebook

Outline:
- Installing Necessary Packages
- Connection
- Basic Queries
  * 
  *

## Installing Necessary Packages 

```{r library}
#install.packages("RMariaDB")
library(DBI)
library(RMariaDB)

# Install config for credentials
# install.packages("config")
```

## Connection
```{r credentials}
db <- config::get("DB")
```


```{r connection}
con <- dbConnect(RMariaDB::MariaDB(),
      user = db$user, password =db$password, dbname = "sakila", port=3306)

rm(db)
```


```{sql, connection=con}
SHOW TABLES
```
```{sql, connection=con, max.print = 20}
SELECT COUNT(*)
FROM film
```
-
